<h2><%= @user.username %></h2>

<h2>Topics</h2>
<ol>
<% @user.topic.each do |t| %>
  <li><%= link_to t.title, topic_path(t) %></li>
<% end %>
</ol>

<h2>Comments</h2>
<ul>
<% @user.comments.each do |t| %>
  <li><%= link_to truncate(c.content), topic_path(c.topic) %></li>
<% end %>
</ul>
